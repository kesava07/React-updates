import React, { Component } from 'react';
import Lazy from './ReactLazy/Lazy';

export default class App extends Component {
  render() {
    return (
      <div>
        <Lazy />
      </div>
    )
  }
};
